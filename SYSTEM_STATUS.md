# 🚀 PUMP DETECTION SYSTEM - СТАТУС УСТАНОВКИ

**Дата установки:** 2025-11-07
**Расположение:** `/home/elcrypto/pump_detector`
**База данных:** `fox_crypto_new.pump` (схема уже создана)

---

## ✅ ЧТО ВЫПОЛНЕНО

### 1. 📁 Структура файлов (ГОТОВО)
```
/home/elcrypto/pump_detector/
├── daemons/                    # 3 демона для мониторинга
│   ├── detector_daemon.py      # Детектор аномалий (каждые 5 мин)
│   ├── validator_daemon.py     # Валидатор сигналов (каждые 15 мин)
│   └── spot_futures_analyzer.py # Анализ корреляции Spot/Futures
├── scripts/                     # Утилиты и скрипты
│   ├── manage_daemons.sh       # Управление демонами
│   ├── calibrate_scoring.py    # Калибровка весов
│   ├── generate_reports.py     # Генерация отчетов
│   ├── test_system.py          # Тестирование системы
│   ├── monitor_dashboard.py    # Real-time мониторинг
│   ├── check_daemons.sh        # Проверка здоровья демонов
│   ├── load_historical_signals.sql # Загрузка исторических данных
│   └── oi_integration.sql      # OI интеграция
├── api/
│   └── web_api.py              # REST API сервер
├── config/
│   └── settings.py             # Конфигурация системы
├── systemd/                    # Файлы для systemd
│   ├── pump-detector.service
│   ├── pump-validator.service
│   └── pump-spot-futures.service
├── logs/                       # Логи системы
├── reports/                    # Отчеты
├── backups/                    # Резервные копии
├── crontab                     # Cron задачи
├── install.sh                  # Скрипт установки
└── requirements.txt            # Python зависимости
```

### 2. 💾 База данных (ГОТОВО)
- **Схема `pump`** с 10 таблицами создана
- **500 исторических сигналов** загружено
- **Функции калибровки** установлены
- **OI интеграция** готова к установке

### 3. ⚙️ Автоматизация (ПОДГОТОВЛЕНО)

#### Systemd сервисы (файлы созданы):
- `pump-detector.service` - основной детектор
- `pump-validator.service` - валидатор
- `pump-spot-futures.service` - анализатор корреляции

#### Cron задачи (файл создан):
- Ежедневная калибровка в 2:00
- Еженедельные отчеты в воскресенье
- Проверка демонов каждые 5 минут
- Ежедневный бэкап в 5:00

---

## 🚦 КАК ЗАПУСТИТЬ

### 1. Быстрый старт (без sudo)
```bash
# Установить зависимости
pip3 install --user -r ~/pump_detector/requirements.txt

# Запустить демоны вручную
~/pump_detector/scripts/manage_daemons.sh start all

# Открыть мониторинг
python3 ~/pump_detector/scripts/monitor_dashboard.py
```

### 2. Production установка (требует sudo)
```bash
# Установить systemd сервисы
sudo cp ~/pump_detector/systemd/*.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable pump-detector pump-validator pump-spot-futures
sudo systemctl start pump-detector pump-validator pump-spot-futures

# Установить cron задачи
crontab ~/pump_detector/crontab
```

### 3. Web API
```bash
# Запустить API сервер
python3 ~/pump_detector/api/web_api.py

# API будет доступен на http://localhost:8000
# Default API key: см. вывод при запуске
```

---

## 📊 ТЕКУЩИЙ СТАТУС СИСТЕМЫ

### База данных
```sql
-- Проверить количество сигналов
SELECT status, COUNT(*) FROM pump.signals GROUP BY status;

-- Активные сигналы
SELECT * FROM pump.signals
WHERE status IN ('DETECTED', 'MONITORING')
ORDER BY signal_timestamp DESC;
```

### Демоны
```bash
# Проверить статус
~/pump_detector/scripts/manage_daemons.sh status all

# Логи
tail -f ~/pump_detector/logs/*.log
```

---

## 📈 МЕТРИКИ ЭФФЕКТИВНОСТИ

Из анализа исторических данных:
- **Общая точность:** 24.6% (500 сигналов)
- **EXTREME (5x+):** 63.8% точность
- **STRONG (3-5x):** 41.2% точность
- **Спайки 100x+:** 80% точность

---

## 🔧 НАСТРОЙКА

### Конфигурация
Редактировать: `~/pump_detector/config/settings.py`
- Интервалы проверки
- Пороги детекции
- Веса скоринга

### Калибровка
```bash
# Запустить калибровку вручную
python3 ~/pump_detector/scripts/calibrate_scoring.py
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **БД уже создана** - схема `pump` существует в `fox_crypto_new`
2. **Системные пути** - все файлы в `~/pump_detector`, не в `/tmp`
3. **Автозапуск** - требует установки systemd сервисов с sudo
4. **Python пакеты** - установить через pip3 перед запуском

---

## 📞 ПОДДЕРЖКА

- **Логи:** `~/pump_detector/logs/`
- **Тесты:** `python3 ~/pump_detector/scripts/test_system.py`
- **Мониторинг:** `python3 ~/pump_detector/scripts/monitor_dashboard.py`

---

## ✅ CHECKLIST

- [x] Файлы перемещены в ~/pump_detector
- [x] БД схема создана и работает
- [x] Systemd сервисы подготовлены
- [x] Cron задачи настроены
- [x] Скрипты исполняемые
- [ ] Python зависимости установлены (pip3 install -r requirements.txt)
- [ ] Демоны запущены
- [ ] API сервер работает

---

**Система готова к запуску!** 🎉